{
  "name": "sift",
  "version": "1.0.0",
  "description": "Sift - A cycle-aware mood evidencing tool",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:ui": "cd public-src && npm install && npm run build && xcopy /E /I dist ..\\public",
    "test": "node --test public-src/logic.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
